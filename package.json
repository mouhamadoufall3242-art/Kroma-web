{
  "name": "kroma",
  "version": "1.0.0",
  "private": true,
  "description": "Kroma — sito, generatore di anteprime AI e checkout Stripe su Cloudflare",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",

    "deploy": "wrangler deploy",
    "deploy:worker": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",

    "tail": "wrangler tail",
    "tail:vendite": "wrangler tail --format pretty --search KROMA-VENDITA",
    "tail:errori": "wrangler tail --format pretty --search KROMA-SOS",

    "kv:create": "wrangler kv namespace create PREVIEWS && wrangler kv namespace create PREVIEWS --preview",

    "secrets": "npm run secret:anthropic && npm run secret:resend && npm run secret:stripe && npm run secret:webhook",
    "secret:anthropic": "wrangler secret put ANTHROPIC_API_KEY",
    "secret:resend": "wrangler secret put RESEND_API_KEY",
    "secret:stripe": "wrangler secret put STRIPE_SECRET_KEY",
    "secret:webhook": "wrangler secret put STRIPE_WEBHOOK_SECRET"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
